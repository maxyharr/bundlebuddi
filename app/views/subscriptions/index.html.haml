%h1
  Available Subscriptions
%table
  %tr
    %th Name
    %th Pre-bundling Cost
  - @subscriptions.each do |subscription|
    %tr
      %td= subscription.name
      %td= number_to_currency subscription.cost_in_cents*100
